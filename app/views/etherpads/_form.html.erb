<%= form_for(@etherpad) do |f| %>
  <% if @etherpad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@etherpad.errors.count, "error") %> prohibited this etherpad from being saved:</h2>

      <ul>
      <% @etherpad.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

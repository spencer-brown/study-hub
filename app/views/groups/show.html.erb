<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @group.name %>
</p>

<p>
	<strong>Etherpads:</strong>
	<% @group.etherpads.each do |etherpad| %>
		<p><%= etherpad.name %></p>
	<% end %>
</p>

<%= link_to 'Edit', edit_group_path(@group) %> |
<%= link_to 'Back', groups_path %>

<button class="<%= @determine ? 'Remove' : 'Add' %>"><%= @determine ? 'Remove' : 'Add' %></button>

<script>
  $('.Add').click(function(){
    var $this = $(this);
    var apply = "<%= apply_group_path(@group) %>"
    var remove = "<%= remove_group_path(@group) %>"
    $this.toggleClass('Add');
    if($this.hasClass('Add')){
      $this.text('Add');
      $.ajax({type: "GET", url: remove});
    } else {
      $this.text('Remove');
      $.ajax({type: "GET", url: apply});
    }
  });

    $('.Remove').click(function(){
    var $this = $(this);
    var apply = "<%= apply_group_path(@group) %>"
    var remove = "<%= remove_group_path(@group) %>"
    $this.toggleClass('Add');
    if($this.hasClass('Add')){
      $this.text('Add');
      $.ajax({type: "GET", url: apply});
    } else {
      $this.text('Remove');
      $.ajax({type: "GET", url: remove});
    }
  });
</script>